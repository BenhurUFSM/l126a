
## Trabalho 3

**A descrição ainda está bem incompleta.**

Implemente um programa de gerenciamento de postits, de acordo com as instruções abaixo.

A implementação deve ser colocada em **um** arquivo, chamado `l1-t3-fulano.c` (com "fulano" substituído pelo que está na planilha de informações dos alunos).
**Anexe esse arquivo** (não cole o conteúdo no texto da mensagem, não anexe uma imagem com o arquivo) em um e-mail e envie para `benhur+l126a@inf.ufsm.br`, com assunto `entrega de l1-t3-fulano`, até o prazo limite.

O programa deve mostrar uma tela com retângulos coloridos "*post-it*s) que podem ser dimensionados e posicionados à vontade pelo operador.
Cada retângulo contém um texto.
O número de postits é qualquer, limitado a um máximo.
As informações sobre os postits é mantida em um arquivo, que deve ser lido no início da execução e atualizado a mando do operador.
Em memória, os postits são mantidos em um vetor. Caso dois postits se sobreponham o que está mais no início do vetor é considerado como estando abaixo do outro.
O vetor de postits e demais informações úteis para seu gerenciamento devem ser mantidos em um registro, que deve ser declarado na `main` e passado por referência para as funções que necessitem.
Um dos postits é considerado o "atual", sobre o qual são realizadas algumas operações (ver abaixo).

Deve ter funções para:
- identificar o postit que está em determinado ponto da tela (visto em aula);
- mover um postit para o final do vetor — recebe o tamanho, o vetor, o índice e move o postit nesse índice para o final, movimentando os demais para não deixar espaço vago no vetor nem alterar a ordem;
- mover um postit para o início do vetor;
- 

